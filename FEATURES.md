# ✨ Xo Studio & Capsule Blog — 全量功能与系统规格总览

> **系统版本**：v4.0 Quantum Matrix / v3.0 ULTRA  
> **定位**：专为独立视频剪辑指导、DI 调色总监与创作者打造的轻奢大刊感个人作品集、胶囊风博客、AI 智能摘要、E2EE 零知识量子安全加密与前端防逆向混淆系统。

---

## 目录
- [一、 🎬 影视与 DI 调色作品集展示模块](#一-🎬-影视与-di-调色作品集展示模块)
- [二、 ✍️ 次世代胶囊博客与 AI 智能总结模块](#二-✍️-次世代胶囊博客与-ai-智能总结模块)
- [三、 🔐 E2EE v4.0 Quantum Matrix 端到端加密体系](#三-🔐-e2ee-v40-quantum-matrix-端到端加密体系)
- [四、 🛡️ v3.0 ULTRA 前端防逆向与防审查系统](#四-🛡️-v30-ultra-前端防逆向与防审查系统)
- [五、 💼 客户合作、审片与在线预约系统](#五-💼-客户合作审片与在线预约系统)
- [六、 ⚙️ 管理后台与可视化监控](#六-⚙️-管理后台与可视化监控)
- [七、 💾 零数据库 Server Disk 持久化架构](#七-💾-零数据库-server-disk-持久化架构)
- [八、 🚀 快速部署与运维指南](#八-🚀-快速部署与运维指南)

---

## 一、 🎬 影视与 DI 调色作品集展示模块

作品集前台为视频剪辑指导与调色总监量身定制，打破传统个人网站展示形式，引入影视级专业交互：

### 1.1 🎞️ Log / Final 调色前后实时对比滑块 (`MediaVideo.vue`)
- **双层视频/图片联动对比**：支持 Log 原始灰片与 Final 最终成片无缝重叠与同步拉伸对比。
- **自定义分割位置**：支持鼠标/触摸拖拽控制分割线，毫秒级响应。
- **全屏预览与放大**：支持全屏无干扰审片模式。

### 1.2 🎵 氛围音效播放器 (`AppNavbar.vue` & `audio-proxy.get.ts`)
- **悬浮音频胶囊**：常驻顶部导航栏，支持轻奢背景音效播放与暂停。
- **频谱/波形动画**：动态 Audio Spectrum 可视化组件，随音频节奏律动。
- **音效代理与缓存**：后端通过 `audio-proxy` 接口进行跨域防盗链代理与高能效流式播放。

### 1.3 🎥 专业级技术规格面板
- **影视级元数据展示**：包括 4K HDR、ProRes 4444 XQ、Rec.709/DCI-P3 色彩空间、分辨率、帧率与制作日期。
- **制作团队职能标示**：清晰标明剪辑指导 (Editor)、调色总监 (Colorist)、导演 (Director)、摄影指导 (DP) 等人员名单。

### 1.4 🍱 Bento 网格与热度洞察 (`BentoContainer.vue` & `BentoItem.vue`)
- **Bento Grid 响应式布局**：融合现代现代大牌美学，支持大块主打作品与小块衍生内容错落排列。
- **点击与播放热度榜**：结合后端 `project-heat.json`，实时统计每个作品的曝光数、点击量与热度榜单。

### 1.5 💧 XO 专属防盗水印合成与验证 (`/xo-watermark`)
- **自定义水印生成器**：支持在线生成加盖创作团队防篡改动态水印。
- **水印校验 API** (`/api/watermark-verify`)：支持密匙与时间戳水印验证。

---

## 二、 ✍️ 次世代胶囊博客与 AI 智能总结模块

### 2.1 📖 前台胶囊博客 (`/blog`)
- **响应式分类筛选**：支持滑动分类胶囊选项卡，动态按类别 (`/blog/category/:category`) 过滤。
- **SEO 友好 Slug 别名**：文章 URL 使用标准 Markdown Slug，自带完美的 Open Graph 元数据。
- **阅读时长预估与字数统计**：自动计算文章阅读时间与全文字数。

### 2.2 🤖 AI 大模型智能文章总结 (`AdminAiCopilot.vue` & `/api/ai/summarize`)
- **多模型无缝对接**：支持在后台配置 **DeepSeek-R1/V3**、**OpenAI GPT-4o** 或 **Google Gemini** API Key 与 Endpoint。
- **抽象归纳 Prompt 优化**：提炼 60-100 字的高质量抽象摘要，拒绝机械照抄首段。
- **打字机打卡呈现**：在前台文章详情页以打字机逐字展现动画打卡展现，伴随精致的 AI 特效 Badge。

### 2.3 📝 双栏 Markdown 编辑器
- **三大创作视图模式**：
  1. ✏️ **纯编辑模式**：极大化专心写作。
  2. ⚖️ **实时双栏对比模式**：左侧 Markdown 实时打字，右侧 1:1 渲染预览。
  3. 👁️ **纯预览模式**：模拟真实文章页面效果。
- **云端与本地文件拖拽上传**：拖拽图片/视频文件自动调用 `/api/upload` 接口生成轻量 URL。
- **封面与头像智能联动**：文章封面支持实时缩放预览与全站个人履历头像联动同步。

---

## 三、 🔐 E2EE v4.0 Quantum Matrix 端到端加密体系

全站搭载基于 W3C 标准 Web Crypto API (`window.crypto.subtle`) 构建的零知识端到端加密体系：

```
[ 明文数据 / 敏感操作 ]
          │
          ▼
   ┌──────────────┐
   │ PBKDF2 100K  │ ◄── 10,0000 次高阶迭代派生 Master Key
   └──────┬───────┘
          │
          ▼
   ┌──────────────┐
   │ HKDF-SHA512  │ ◄── RFC 5869 标准派生 Encrypt Key 与 HMAC Key
   └──────┬───────┘
          │
     ┌────┴──────────────────────────┐
     ▼                               ▼
┌──────────────┐            ┌──────────────────┐
│ AES-256-GCM  │            │ HMAC-SHA512 签名 │ ◄── 48 字节密文校验
└──────┬───────┘            └────────┬─────────┘
       │                             │
       └──────────────┬──────────────┘
                      ▼
        [ ZKP-SHA512 零知识密文传输 ]
```

### 3.1 核心加密技术指标
1. **PBKDF2 100,000 次高阶迭代**：抵御 GPU 暴破解密，使用 SHA-256 生成强派生主密钥。
2. **HKDF-SHA512 密钥展开**：遵循 RFC 5869 标准，分离解密密钥与 HMAC 签名密钥。
3. **AES-256-GCM + 12-byte 随机 IV**：保障密文的机密性与认证性。
4. **ZKP-SHA512 零知识证明**：身份校验无需传输任何明文密码凭证。

### 3.2 🔐 后台 E2EE 安全沙盒控制台 (`AdminSecurityGateway.vue`)
- **512 位量子安全指纹展示**：直观输出当前加密会话的量子级 Hash 校验码。
- **一键热轮换 AES 密钥**：后台支持一键触发 `Rotate Keys`，瞬间重新加密全站敏感数据并作废旧 Session。
- **加解密沙盒测试工具**：允许管理员在线输入字符串进行实时 AES-256-GCM 密文生成与解密验证。

---

## 四、 🛡️ v3.0 ULTRA 前端防逆向与防审查系统

为防止作品集与博客内容被非法抓取、逆向工程与代码盗用，部署了体系化防审查防御：

| 防御维度 | 实现技术 | 说明 |
| :--- | :--- | :--- |
| **代码混淆** | Terser 5-Pass AST 重构 (`passes: 5`) | `unsafe` & `toplevel` 算法，变量与函数名全面单字符 Hash 化 |
| **Source Map 剥离** | 关停 `.map` 生成 | 物理切断浏览器源码映射，Rollup 块加密重命名为 `[hash:16].js` |
| **防右键/防审查** | 全局 `contextmenu` 拦截 | 禁用右键菜单，彻底屏蔽“检查”、“查看网页源代码”与“另存为” |
| **防拖拽与复制** | CSS & JS 行为锁 | 物理禁止页面文本选中、图片拖拽与剪切板复制 |
| **DevTools 陷阱** | Infinite Debugger | 高频触发 `Function("debugger")()`，打开开发者工具即导致面板卡死 |

---

## 五、 💼 客户合作、审片与在线预约系统

### 5.1 🔒 客户专区与密匙审片 (`/client` & `ClientTokenGuard.vue`)
- **客户 Token 凭证防护**：特定商业项目支持凭证 Token 解锁。
- **私密审片与素材交付**：提供未公开水印样片预览、客户反馈提交与源文件交付下载通道。

### 5.2 📅 商业合作预订通道 (`/booking`)
- **档期与需求提交**：在线挑选剪辑/调色服务类型、预计预算区间、交付时间节点。
- **预约邮件/消息通知**：提交数据直接录入后台待处理队列 ([booking.post.ts](file:///d:/Git/zpj/server/api/booking.post.ts))。

### 5.3 🔑 密码找回与审核重置工作流
- **申请重置通道**：前台发起密码重置请求 ([password-requests.post.ts](file:///d:/Git/zpj/server/api/password-requests.post.ts))。
- **管理员安全控制台响应**：后台可一键批准解绑或拒绝非法找回请求。

---

## 六、 ⚙️ 管理后台与可视化监控

### 6.1 🕵️ 自定义后台入口路径 (`/[adminSuffix]`)
- **URL 隐藏机制**：远离通用 `/admin` 漏洞扫描路径，可自由定义高强度隐藏 URL 别名。
- ** Session 强制重打卡**：敏感页隔离开启超时重认证防护。

### 6.2 📊 流量与热度大盘 (`/api/stats`)
- **全站 PV / UV 实时折线图**。
- **作品集播放次数与热度排行榜**。

### 6.3 🖥️ 系统健康度大盘 (`system-status.get.ts`)
- **服务器内存 & CPU Load 监控**。
- **磁盘占用空间与系统 Node.js 运行时长**。

### 6.4 🎨 主题色彩控制面板 (`AdminThemePalette.vue`)
- **物理天蓝 (`#007AFF`) / 高阶黑灰暗黑模式切换**。
- **品牌高光色与全局字体调整**。

---

## 七、 💾 零数据库 Server Disk 持久化架构

无传统关系型数据库维护成本，数据文件独立保存在服务器磁盘 `content/` 目录中，支持跨设备与全网实时同步，**不会被 `git pull` 覆盖**：

```
content/
├── blog-posts.json           # 博客文章持久化数据
├── blog-categories.json      # 博客分类与标签映射
├── site-config.json          # 站点名称、AI 大模型 Key 与配置
├── project-heat.json         # 浏览量、点击热度与播放统计
└── projects/                 # 视频作品数据
```

---

## 八、 🚀 快速部署与运维指南

```bash
# 1. 克隆代码库
git clone https://github.com/your-username/xo-studio.git
cd xo-studio

# 2. 安装依赖
npm install

# 3. 本地开发调试
npm run dev

# 4. 生产构建（自动触发 Terser 5-Pass 混淆与 Source Map 剥离）
npm run build

# 5. 生产环境预览
npm run preview
```

> **系统要求**：Node.js >= 18.0，npm >= 9.0  
> **更多详细代码实现与 API 规范请参考源码对应模块。**
